# OpenAI is the default provider for the bundled model strings.
# Set the key for whichever provider you switch to in src/agents/*.
OPENAI_API_KEY=sk-...

# Optional: a different model can be wired through env if you want to override
# the hard-coded model strings in the agent files at runtime.
MASTRA_MODEL_DEFAULT=openai/gpt-4o-mini
